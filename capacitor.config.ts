import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.derrick.playground',
  appName: 'Playground',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
